# Руководство по использованию Microsoft Agent Framework - Magentic Workflow

## 📖 Содержание

1. [Быстрый старт](#быстрый-старт)
2. [Типы Workflow](#типы-workflow)
3. [Подробное описание компонентов](#подробное-описание-компонентов)
4. [Создание своих workflow](#создание-своих-workflow)
5. [Настройка агентов](#настройка-агентов)
6. [Визуализация и отладка](#визуализация-и-отладка)
7. [FAQ](#faq)
8. [Решение проблем](#решение-проблем)

---

## 🚀 Быстрый старт

### Шаг 1: Установка

```bash
# Linux/Mac
chmod +x setup.sh
./setup.sh

# Windows
setup.bat
```

### Шаг 2: Настройка API ключа

Отредактируйте `appsettings.json`:

```json
{
  "OpenAI": {
    "ApiKey": "sk-ваш-реальный-ключ-здесь"
  }
}
```

### Шаг 3: Запуск

```bash
# Простой пример
dotnet run workflow-simple.json

# Sequential workflow
dotnet run workflow-sequential.json

# Concurrent workflow
dotnet run workflow-concurrent.json

# Conditional workflow
dotnet run workflow-conditional.json
```

---

## 🔀 Типы Workflow

### 1. Sequential (Последовательный)

**Концепция:** Агенты выполняются один за другим в строгом порядке. Выход каждого агента передается на вход следующему.

**Преимущества:**
- ✅ Предсказуемый порядок выполнения
- ✅ Легко отлаживать
- ✅ Подходит для задач с четкими этапами
- ✅ Эффективное использование ресурсов

**Недостатки:**
- ❌ Медленнее параллельного выполнения
- ❌ Весь pipeline блокируется на медленных агентах
- ❌ Нет гибкости в маршрутизации

**Когда использовать:**
- Обработка данных (сбор → очистка → анализ → отчет)
- Создание контента (исследование → написание → редактура → публикация)
- Валидация (проверка → исправление → повторная проверка)

**Пример:**
```json
{
  "workflowType": "Sequential",
  "orchestration": {
    "startAgent": "CollectorAgent",
    "edges": [
      { "from": "CollectorAgent", "to": "ProcessorAgent" },
      { "from": "ProcessorAgent", "to": "ReporterAgent" }
    ]
  }
}
```

### 2. Concurrent (Параллельный)

**Концепция:** Все агенты получают одинаковую задачу и выполняются одновременно. Результаты агрегируются.

**Преимущества:**
- ✅ Высокая скорость выполнения
- ✅ Использует множественные перспективы
- ✅ Отказоустойчивость (если один агент сбоит)
- ✅ Можно сравнивать результаты

**Недостатки:**
- ❌ Выше стоимость (больше API вызовов)
- ❌ Нужна логика агрегации
- ❌ Возможны противоречивые результаты

**Стратегии агрегации:**
- **Collect** - собрать все ответы
- **Merge** - объединить в один документ
- **Vote** - выбрать лучший (консенсус)

**Когда использовать:**
- Анализ с разных точек зрения
- Обработка независимых подзадач
- Генерация вариантов решений
- Проверка качества (множественные ревьюеры)

**Пример:**
```json
{
  "workflowType": "Concurrent",
  "orchestration": {
    "concurrent": {
      "participantAgents": ["HealthcareAgent", "FinanceAgent", "EducationAgent"],
      "aggregationStrategy": "Merge"
    }
  }
}
```

### 3. Conditional (Условный)

**Концепция:** Маршрутизация зависит от условий и промежуточных результатов. Динамическое принятие решений.

**Преимущества:**
- ✅ Гибкая логика выполнения
- ✅ Адаптируется к данным
- ✅ Эффективное использование агентов
- ✅ Может обрабатывать исключения

**Недостатки:**
- ❌ Сложнее проектировать
- ❌ Труднее предсказать путь выполнения
- ❌ Нужны хорошие условия маршрутизации

**Когда использовать:**
- Системы обработки запросов (routing по типу)
- Workflows с исключениями и эскалацией
- Адаптивные процессы
- Тriage и классификация

**Пример:**
```json
{
  "workflowType": "Conditional",
  "orchestration": {
    "startAgent": "ClassifierAgent",
    "conditionalEdges": [
      {
        "from": "ClassifierAgent",
        "toOptions": ["TechnicalAgent", "BillingAgent"],
        "selectionFunction": "classify_ticket"
      }
    ]
  }
}
```

### 4. Magentic (Динамический)

**Концепция:** Менеджер наблюдает за выполнением и динамически выбирает, какой агент должен действовать следующим.

**Преимущества:**
- ✅ Максимальная гибкость
- ✅ Итеративное улучшение
- ✅ Адаптируется к сложным задачам
- ✅ Может исправлять ошибки

**Недостатки:**
- ❌ Самый дорогой (много вызовов менеджера)
- ❌ Непредсказуемое время выполнения
- ❌ Сложнее отлаживать

**Когда использовать:**
- Сложные исследовательские задачи
- Задачи с неизвестным решением
- Требуется креативность и адаптация
- Мультимодальные задачи

**Пример:**
```json
{
  "workflowType": "Magentic",
  "manager": {
    "maxRoundCount": 10,
    "maxStallCount": 3,
    "maxResetCount": 2
  }
}
```

---

## 🎯 Сравнение типов workflow

| Характеристика | Sequential | Concurrent | Conditional | Magentic |
|----------------|------------|------------|-------------|----------|
| Скорость | 🟡 Средняя | 🟢 Быстрая | 🟡 Средняя | 🔴 Медленная |
| Стоимость | 🟢 Низкая | 🔴 Высокая | 🟡 Средняя | 🔴 Очень высокая |
| Предсказуемость | 🟢 Высокая | 🟢 Высокая | 🟡 Средняя | 🔴 Низкая |
| Гибкость | 🔴 Низкая | 🔴 Низкая | 🟢 Высокая | 🟢 Очень высокая |
| Сложность | 🟢 Простая | 🟡 Средняя | 🔴 Сложная | 🔴 Очень сложная |
| Отладка | 🟢 Легко | 🟡 Средне | 🔴 Сложно | 🔴 Очень сложно |

---

## 📚 Подробное описание компонентов

### 1. WorkflowConfiguration

Корневой объект конфигурации, содержащий все настройки workflow.

**Поля:**
- `workflowType`: Тип оркестрации (в данный момент поддерживается "Magentic")
- `task`: Описание задачи для выполнения
- `manager`: Конфигурация менеджера
- `agents`: Массив агентов
- `settings`: Дополнительные настройки

### 2. Manager (Менеджер)

Координирует работу агентов, принимает решения о том, какой агент должен действовать следующим.

**Параметры:**

| Параметр | Тип | Описание | Рекомендации |
|----------|-----|----------|--------------|
| `modelId` | string | Модель OpenAI для менеджера | Используйте "gpt-4" для лучшего планирования |
| `maxRoundCount` | int | Максимум раундов | 5-15 для большинства задач |
| `maxStallCount` | int | Раундов без прогресса до сброса | 2-4 обычно достаточно |
| `maxResetCount` | int | Максимум сбросов плана | 1-3 для сложных задач |
| `enablePlanReview` | bool | Включить ревью плана человеком | true для критических задач |

**Примеры настроек:**

Простая задача:
```json
{
  "modelId": "gpt-4",
  "maxRoundCount": 5,
  "maxStallCount": 2,
  "maxResetCount": 1,
  "enablePlanReview": false
}
```

Сложная задача:
```json
{
  "modelId": "gpt-4",
  "maxRoundCount": 15,
  "maxStallCount": 4,
  "maxResetCount": 3,
  "enablePlanReview": true
}
```

### 3. Agents (Агенты)

Специализированные исполнители, каждый со своей областью компетенции.

**Обязательные поля:**
- `name`: Уникальное имя (например, "ResearcherAgent")
- `description`: Краткое описание специализации
- `instructions`: Детальные инструкции для агента
- `modelId`: Модель OpenAI для агента

**Опциональные поля:**
- `tools`: Массив инструментов (например, ["CodeInterpreter"])
- `metadata`: Дополнительная информация

**Типы агентов:**

#### Исследователь (Researcher)
```json
{
  "name": "ResearcherAgent",
  "description": "Specialist in research and information gathering",
  "instructions": "You find information without computation. Focus on facts and data.",
  "modelId": "gpt-4o-search-preview",
  "tools": []
}
```

#### Аналитик данных (Data Analyst)
```json
{
  "name": "DataAnalystAgent",
  "description": "Analyzes data and creates insights",
  "instructions": "Analyze data using statistical methods. Create visualizations.",
  "modelId": "gpt-4",
  "tools": ["CodeInterpreter"]
}
```

#### Программист (Coder)
```json
{
  "name": "CoderAgent",
  "description": "Writes and executes code",
  "instructions": "Write clean, efficient code. Include error handling.",
  "modelId": "gpt-4",
  "tools": ["CodeInterpreter"]
}
```

---

## 🔨 Создание своих workflow

### Шаблон для новой задачи

```json
{
  "workflowType": "Magentic",
  "task": "ВАША_ЗАДАЧА_ЗДЕСЬ",
  "manager": {
    "modelId": "gpt-4",
    "maxRoundCount": 10,
    "maxStallCount": 3,
    "maxResetCount": 2,
    "enablePlanReview": false
  },
  "agents": [
    {
      "name": "Agent1",
      "description": "Описание агента 1",
      "instructions": "Детальные инструкции",
      "modelId": "gpt-4",
      "tools": [],
      "metadata": {}
    }
  ],
  "settings": {}
}
```

### Примеры задач по категориям

#### 1. Исследовательские задачи

```json
{
  "task": "Исследуйте текущее состояние квантовых вычислений и их практическое применение в 2024 году",
  "agents": [
    {
      "name": "AcademicResearcher",
      "description": "Searches academic papers and sources",
      "instructions": "Find academic papers, research studies, and expert opinions",
      "modelId": "gpt-4o-search-preview"
    },
    {
      "name": "IndustryAnalyst",
      "description": "Analyzes industry trends and applications",
      "instructions": "Focus on commercial applications and industry adoption",
      "modelId": "gpt-4"
    }
  ]
}
```

#### 2. Аналитические задачи

```json
{
  "task": "Проанализируйте данные продаж за год и выявите тренды",
  "agents": [
    {
      "name": "DataCleanerAgent",
      "description": "Cleans and prepares data",
      "instructions": "Handle missing values, outliers, normalize data",
      "modelId": "gpt-4",
      "tools": ["CodeInterpreter"]
    },
    {
      "name": "StatisticalAnalystAgent",
      "description": "Performs statistical analysis",
      "instructions": "Calculate trends, correlations, significance tests",
      "modelId": "gpt-4",
      "tools": ["CodeInterpreter"]
    },
    {
      "name": "VisualizationAgent",
      "description": "Creates charts and visualizations",
      "instructions": "Create clear, informative visualizations",
      "modelId": "gpt-4",
      "tools": ["CodeInterpreter"]
    }
  ]
}
```

#### 3. Креативные задачи

```json
{
  "task": "Разработайте маркетинговую кампанию для нового продукта",
  "agents": [
    {
      "name": "MarketResearcher",
      "description": "Researches target audience",
      "instructions": "Identify target demographics, pain points, preferences",
      "modelId": "gpt-4"
    },
    {
      "name": "CreativeWriter",
      "description": "Creates marketing copy",
      "instructions": "Write compelling, engaging marketing content",
      "modelId": "gpt-4"
    },
    {
      "name": "StrategyPlanner",
      "description": "Plans marketing strategy",
      "instructions": "Develop channel strategy, budget allocation, timeline",
      "modelId": "gpt-4"
    }
  ]
}
```

---

## 🎨 Визуализация и отладка

### Визуализация workflow

Приложение автоматически создает визуализацию перед запуском:

1. **Структурная визуализация**
   - Тип workflow
   - Описание задачи
   - Конфигурация менеджера
   - Список агентов с описаниями

2. **Mermaid диаграмма**
   - Граф взаимодействия компонентов
   - Потоки данных между агентами

### События в консоли

```
[ORCHESTRATOR:Planning] - Менеджер планирует действия
[ORCHESTRATOR:Progress] - Оценка прогресса
[AGENT:AgentName] - Сообщения от агента
[STREAM:AgentName] - Потоковый вывод
[FINAL RESULT] - Итоговый результат
```

### Уровни логирования

В `appsettings.json`:

```json
{
  "Logging": {
    "LogLevel": {
      "Default": "Information",    // Стандартные сообщения
      "Microsoft": "Warning",       // Только предупреждения от фреймворка
      "System": "Warning"
    }
  }
}
```

Для отладки:
```json
{
  "Logging": {
    "LogLevel": {
      "Default": "Debug"  // Детальная информация
    }
  }
}
```

---

## ❓ FAQ

### Q: Нужно ли устанавливать Microsoft.Agents.* пакеты?

**A:** Да, для работы с реальным API вам нужны:
```bash
dotnet add package Microsoft.Agents.AI --prerelease
dotnet add package Microsoft.Agents.AI.Workflows --prerelease
dotnet add package Microsoft.Agents.OpenAI --prerelease
```

Текущий код содержит заглушку с демонстрацией, но для полной функциональности нужны настоящие пакеты.

### Q: Можно ли использовать Azure OpenAI вместо OpenAI?

**A:** Да, настройте в `appsettings.json`:
```json
{
  "AzureOpenAI": {
    "Endpoint": "https://your-resource.openai.azure.com/",
    "ApiKey": "your-key",
    "DeploymentName": "gpt-4"
  }
}
```

### Q: Как ограничить стоимость выполнения?

**A:** Используйте параметры:
- `maxRoundCount` - ограничивает количество раундов
- Выбирайте более дешевые модели (gpt-3.5-turbo вместо gpt-4)
- Делайте задачи более конкретными

### Q: Можно ли добавить своих агентов?

**A:** Да! Просто добавьте в массив `agents`:
```json
{
  "name": "CustomAgent",
  "description": "Your custom agent",
  "instructions": "Custom instructions",
  "modelId": "gpt-4"
}
```

### Q: Как работает `enablePlanReview`?

**A:** Когда включено, менеджер будет запрашивать подтверждение плана у пользователя перед выполнением. Вы сможете одобрить, отклонить или изменить план.

### Q: Какие инструменты доступны агентам?

**A:** Сейчас поддерживается:
- `CodeInterpreter` - для выполнения Python кода

В будущем могут быть добавлены:
- Web search
- File access
- Custom tools

---

## 🔧 Решение проблем

### Проблема: "API key not found"

**Решение:**
1. Проверьте `appsettings.json`
2. Или установите переменную окружения:
   ```bash
   export OpenAI__ApiKey="your-key"
   ```

### Проблема: "Failed to restore packages"

**Решение:**
```bash
# Очистка и восстановление
dotnet clean
dotnet restore --force
```

### Проблема: "Workflow стопится (stalls)"

**Решение:**
1. Увеличьте `maxStallCount`
2. Сделайте задачу более конкретной
3. Добавьте больше специализированных агентов
4. Уточните инструкции для агентов

### Проблема: Долгое выполнение

**Решение:**
1. Уменьшите `maxRoundCount`
2. Используйте меньше агентов
3. Упростите задачу
4. Добавьте более конкретные инструкции

### Проблема: Высокая стоимость

**Решение:**
1. Используйте gpt-3.5-turbo вместо gpt-4
2. Уменьшите `maxRoundCount`
3. Делайте задачи более простыми и конкретными
4. Тестируйте на простых примерах сначала

---

## 📊 Метрики и мониторинг

### Отслеживание использования

Логируются:
- Количество раундов
- Количество вызовов агентов
- Время выполнения
- События сброса плана

### Пример вывода

```
[ORCHESTRATOR:Planning] Round 1/10
[AGENT:ResearcherAgent] Gathering information...
[AGENT:CoderAgent] Processing data...
[ORCHESTRATOR:Progress] 30% complete
...
[FINAL RESULT] Task completed in 5 rounds
```

---

## 🎯 Best Practices

1. **Специализация агентов**
   - Каждый агент должен иметь четкую специализацию
   - Избегайте дублирования функций

2. **Четкие инструкции**
   - Пишите конкретные, детальные инструкции
   - Включайте примеры желаемого поведения

3. **Правильный выбор моделей**
   - gpt-4: для сложных рассуждений
   - gpt-4o-search-preview: для поиска информации
   - gpt-3.5-turbo: для простых задач

4. **Оптимизация параметров**
   - Начинайте с низких значений `maxRoundCount`
   - Увеличивайте постепенно по необходимости

5. **Тестирование**
   - Тестируйте на простых примерах
   - Постепенно усложняйте задачи
   - Используйте DEMO режим для проверки конфигурации

---

## 📝 Changelog

### Version 1.0.0
- Начальная реализация
- Поддержка Magentic orchestration
- Загрузка конфигурации из JSON
- Визуализация workflow
- Консольное логирование

---

## 🤝 Поддержка

Если у вас возникли вопросы:
1. Проверьте этот guide
2. Изучите примеры в директории
3. Проверьте официальную документацию Microsoft

---

**Желаем успешной работы с Microsoft Agent Framework! 🚀**
